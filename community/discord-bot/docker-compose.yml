version: '3.8'

services:
  bot:
    build: .
    container_name: devsecops-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./bot.log:/app/bot.log
    environment:
      - PYTHONUNBUFFERED=1